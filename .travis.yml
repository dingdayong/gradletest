language: java
install: skip

os: linux
dist: trusty
jdk: oraclejdk8

script:
  - gradle build --scan -s

deploy:
  provider: releases
  api_key:
    secure: "$GITHUB_TOKEN"
  skip_cleanup: true
  on:
    tags: false